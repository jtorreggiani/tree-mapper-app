<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <br/>
    <%= form.submit %>
  </div>

  <div class="field">
    <%= form.text_field :title, class: 'title-input', placeholder: 'Title' %>
  </div>

  <div class="field">
    <%= form.text_area :body, class: 'body-input', placeholder: 'In a galaxy far way...' %>
  </div>
<% end %>
